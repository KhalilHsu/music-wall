{
  "name": "music-wall",
  "version": "0.1.0",
  "description": "Local-first animated album artwork wall for the browser.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check app.js && node --check server.mjs",
    "start": "node server.mjs"
  }
}
